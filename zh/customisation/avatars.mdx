---
title: 头像
---

默认助手头像是应用的 favicon。请参阅[此处](/customisation/custom-logo-and-favicon)了解如何自定义 favicon。

不过，你可以通过将图像文件放在 `/public/avatars` 文件夹中来自定义头像。
图像文件应以消息作者命名。例如，若作者为 `My Assistant`，头像应命名为 `my_assistant.png`。

```
public/
└── avatars/
    └── my_assistant.png
```
